// create a new component. this component
// should produce some HTML

const App = function() {
	return <div>Hi!</div>; //<- JSX; subset of JS
}

// take this component's generated HTML
// and put it on the page (in the DOM)